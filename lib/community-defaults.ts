// 교제 게시판 기본 카테고리.
// 교회가 관리자에서 카테고리를 직접 정의하면 그 값이 우선 사용되고,
// 정의된 카테고리가 없을 때 아래 기본값이 노출되어 바로 사용할 수 있다.
export const DEFAULT_COMMUNITY_CATEGORIES = [
  "간증",
  "감사",
  "일상 나눔",
  "기도 부탁",
  "질문",
  "자유",
] as const;
